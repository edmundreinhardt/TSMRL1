      /* METADATA                                                     */
      /* %TEXT TEXT(Create file with description of ARTILE)           */

             PGM
             DSPFD      FILE(ARTICLE) TYPE(*BASATR) OUTPUT(*OUTFILE) +
                          OUTFILE(QTEMP/QDSPFD)
             ENDPGM
